{
  "name": "tg-webapp",
  "version": "2.0.0",
  "description": "tg-webapp.web.app",
  "source": [
    "index.html"
  ],
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "rm -fdr dist && parcel build --no-scope-hoist",
    "deploy": "firebase deploy --project 998906531698 --only hosting"
  },
  "dependencies": {
    "arrmatura-ui": "workspace:",
    "arrmatura-web": "workspace:",
    "ultimus": "workspace:"
  },
  "devDependencies": {
    "postcss": "^8.4.19",
    "tailwindcss": "3.3.2"
  }
}
